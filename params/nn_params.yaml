data_collection_duration: 30 # seconds
num_of_iterations: 1 # number of iterations for training - increase for precision, but be wary of overfitting
num_of_epochs: 100 # number of epochs for training at each iteration
lr: 0.0005 # learning rate
weight_decay: 0.0 # weight decay